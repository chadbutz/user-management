CREATE TABLE users (ID INT PRIMARY KEY auto_increment, FIRST_NAME VARCHAR(64), LAST_NAME VARCHAR(64));

INSERT INTO users (first_name, last_name) VALUES ('test first', 'test last'), ('test first1', 'test last1');